sumCertainCols(data.table::data.table(iris), id.col = "Species", search.string = "Petal", onlyTotal = FALSE)

sumCertainCols(data.table::data.table(iris), id.col = "Species", search.string = "Petal", onlyTotal = TRUE)
